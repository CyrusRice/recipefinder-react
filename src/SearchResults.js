const SearchResults = (searchString) => {
  return ( 
    <div className="container" id="container"></div>
   );
}
 
export default SearchResults;

// Use searchString to dynamically display grid of results